square :: Int -> Int
square n = n * n

cube :: Int -> Int
cube n = n * n * n

volume :: Float -> Float 
volume r = (4 / 3) * pi * (r ^ 3)

surfacearea :: Float -> Float
surfacearea r = 4 * pi * (r ^ 2)
